spring:
  profiles:
    active: test
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
